import { Card } from '@/components/ui/card';
import { ProcessedData } from '@/types/data';

export default function Tab(props: any) {
  return (
    <Card className="p-6">
      <h2 className="text-2xl font-bold mb-4">Componente em desenvolvimento</h2>
      <p className="text-gray-600">Esta aba será implementada em breve...</p>
    </Card>
  );
}
